<UserControl x:Class="SportsClubManagement.Views.TeamsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SportsClubManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:TeamsViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Các Team của tôi" FontSize="24" FontWeight="Bold" 
                       Foreground="#1976D2" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Create Team Section -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Tạo Team Mới" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0" Margin="0,0,10,0">
                        <TextBox Text="{Binding NewTeamName, UpdateSourceTrigger=PropertyChanged}" 
                                 Padding="8" x:Name="TeamNameTextBox"/>
                        <TextBlock Text="Nhập tên team..." Foreground="Gray" Padding="10,8" 
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=TeamNameTextBox, Path=Text}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    
                    <Grid Grid.Column="1" Margin="0,0,10,0">
                        <TextBox Text="{Binding NewTeamDescription, UpdateSourceTrigger=PropertyChanged}" 
                                 Padding="8" x:Name="TeamDescTextBox"/>
                        <TextBlock Text="Nhập mô tả (không bắt buộc)..." Foreground="Gray" Padding="10,8" 
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ElementName=TeamDescTextBox, Path=Text}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    
                    <Button Grid.Column="2" Content="➕ Tạo Team" Command="{Binding CreateTeamCommand}"
                            Padding="12,8" Background="#4CAF50" Foreground="White" 
                            FontWeight="SemiBold" Cursor="Hand"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Teams List -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Teams}" AutoGenerateColumns="False" 
                  SelectedItem="{Binding SelectedTeam}" CanUserAddRows="False"
                  Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên Team" Binding="{Binding Team.Name}" Width="200"/>
                <DataGridTextColumn Header="Mô tả" Binding="{Binding Team.Description}" Width="*"/>
                <DataGridTextColumn Header="Vai trò" Binding="{Binding MemberRole}" Width="100"/>
                <DataGridTextColumn Header="Thành viên" Binding="{Binding MemberCount}" Width="80"/>
                <DataGridTemplateColumn Header="Hành động" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Xem chi tiết" Padding="8,5" 
                                    Background="#2196F3" Foreground="White" Cursor="Hand"
                                    Command="{Binding DataContext.ViewTeamCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
