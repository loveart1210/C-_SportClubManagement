<UserControl x:Class="SportsClubManagement.Views.PersonalScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SportsClubManagement.ViewModels"
             xmlns:conv="clr-namespace:SportsClubManagement.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:PersonalScheduleViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <conv:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Lịch Tập Cá Nhân" FontSize="24" FontWeight="Bold" Foreground="#1976D2" VerticalAlignment="Center"/>
            
            <!-- Filters (only visible/relevant for the second tab) -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=1}">
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Trạng thái" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding FilterStatus}" Width="100" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Ngày" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding DayOptions}" SelectedItem="{Binding FilterDay}" Width="65" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Tháng" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding MonthOptions}" SelectedItem="{Binding FilterMonth}" Width="65" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Năm" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding YearOptions}" SelectedItem="{Binding FilterYear}" Width="80" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Buổi tập" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding SessionNameOptions}" SelectedItem="{Binding FilterSessionName}" Width="110" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Môn" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding SubjectNameOptions}" SelectedItem="{Binding FilterSubjectName}" Width="110" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Bắt đầu" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding StartTimeOptions}" SelectedItem="{Binding FilterStartTime}" Width="80" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Margin="5,0,0,0">
                    <TextBlock Text="Kết thúc" FontSize="11" Foreground="Gray" Margin="2,0,0,2"/>
                    <ComboBox ItemsSource="{Binding EndTimeOptions}" SelectedItem="{Binding FilterEndTime}" Width="80" VerticalContentAlignment="Center"/>
                </StackPanel>
                <Button Content="Clear" Command="{Binding ClearFiltersCommand}" Margin="10,0,0,0" Padding="10,5" VerticalAlignment="Bottom" Background="#9E9E9E" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </DockPanel>

        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
            <!-- Subjects Tab -->
            <TabItem Header="Môn tập cá nhân">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- List Subjects -->
                    <DockPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock DockPanel.Dock="Top" Text="Danh sách môn tập" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ListBox ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" BorderBrush="#DDD" HorizontalContentAlignment="Stretch">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="Gray"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="✏️ Sửa" Command="{Binding DataContext.EditSubjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" Padding="8,4" Margin="0,0,5,0"
                                                    Background="#2196F3" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="🗑️ Xóa" Command="{Binding DataContext.RemoveSubjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}" Padding="8,4" Background="#FF5252" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>

                    <!-- Create Subject -->
                    <Border Grid.Column="1" Background="#F9F9F9" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Thêm môn tập mới" FontWeight="Bold" Margin="0,0,0,15"/>
                            
                            <TextBlock Text="Tên môn tập" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding NewSubjectName, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,10"/>
                            
                            <TextBlock Text="Mô tả" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding NewSubjectDesc}" Padding="5" Margin="0,0,0,15" Height="60"/>
                            
                            <Button Content="Thêm Môn Tập" Command="{Binding AddSubjectCommand}" 
                                    Padding="10" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Schedule/Calendar Tab -->
            <TabItem Header="Lịch tập cá nhân">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sidebar Controls -->
                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <Calendar SelectedDate="{Binding SelectedDate}" Margin="0,0,0,20"/>
                        
                        <Border Background="#F9F9F9" Padding="15" CornerRadius="5">
                            <StackPanel>
                                <TextBlock Text="Thêm buổi tập" FontWeight="Bold" Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Tên buổi tập" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding NewSessionName, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Môn tập" Margin="0,0,0,5"/>
                                <ComboBox ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}" DisplayMemberPath="Name" Margin="0,0,0,10" Padding="5"/>
                                
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                        <TextBlock Text="Bắt đầu" FontSize="11"/>
                                        <TextBox Text="{Binding NewSessionStart}" Padding="5"/> 
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                        <TextBlock Text="Kết thúc" FontSize="11"/>
                                        <TextBox Text="{Binding NewSessionEnd}" Padding="5"/>
                                    </StackPanel>
                                </Grid>
                                
                                <TextBlock Text="Ghi chú" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding NewSessionNote}" Padding="5" Margin="0,0,0,10"/>
                                
                                <Button Content="Thêm Buổi Tập" Command="{Binding AddSessionCommand}" 
                                        Background="#2196F3" Foreground="White" Padding="10" 
                                        BorderThickness="0" Margin="0,0,0,10"/>

                                <Button Content="Xuất Lịch (CSV)" Command="{Binding ExportScheduleCommand}"
                                        Background="#FF9800" Foreground="White" Padding="10" BorderThickness="0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Sessions List -->
                    <DockPanel Grid.Column="1">
                        <TextBlock DockPanel.Dock="Top" Text="{Binding SelectedDate, StringFormat='Danh sách buổi tập - {0:dd/MM/yyyy}'}" 
                                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                        
                        <DataGrid ItemsSource="{Binding SessionViewItems}" AutoGenerateColumns="False" CanUserAddRows="False" 
                                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="White" BorderBrush="#EEE"
                                  CanUserResizeColumns="True" SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ngày" Binding="{Binding Session.StartTime, StringFormat=dd/MM/yyyy}" Width="85" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Bắt đầu" Binding="{Binding Session.StartTime, StringFormat=HH:mm}" Width="65" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Kết thúc" Binding="{Binding Session.EndTime, StringFormat=HH:mm}" Width="65" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Tên buổi tập" Binding="{Binding Session.Name}" Width="*" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Môn" Binding="{Binding SubjectName}" Width="100" IsReadOnly="True"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Hoàn thành">
                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Quá hạn">
                                                    <Setter Property="Foreground" Value="#F44336"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="Điểm danh" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Điểm danh" 
                                                    Command="{Binding DataContext.AttendSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}"
                                                    Background="{Binding AttendanceColor}"
                                                    IsEnabled="{Binding IsAttendanceEnabled}"
                                                    Padding="10,5" BorderBrush="#CCC" BorderThickness="1" Cursor="Hand"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Width="50">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="🗑️" Command="{Binding DataContext.RemoveSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding}" Background="Transparent" Foreground="Red" BorderThickness="0" FontSize="14" Cursor="Hand"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                    </DockPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
