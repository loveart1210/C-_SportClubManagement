<UserControl x:Class="SportsClubManagement.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SportsClubManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:DashboardViewModel />
    </UserControl.DataContext>

    <Grid>
        <StackPanel VerticalAlignment="Top">
            <!-- Welcome Message -->
            <TextBlock Text="{Binding WelcomeMessage}" 
                       FontSize="28" FontWeight="Bold" 
                       Foreground="#1976D2" Margin="0,0,0,30"/>
            
            <!-- Stats Cards -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Card 1: Teams -->
                <Border Grid.Column="0" Background="#E3F2FD" CornerRadius="8" Padding="20" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="👥 Teams" FontSize="16" FontWeight="Bold" Foreground="#1976D2" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding TotalTeams}" FontSize="32" FontWeight="Bold" Foreground="#0D47A1"/>
                        <TextBlock Text="Team bạn tham gia" Foreground="#666" FontSize="12"/>
                    </StackPanel>
                </Border>
                
                <!-- Card 2: Upcoming Sessions -->
                <Border Grid.Column="1" Background="#F3E5F5" CornerRadius="8" Padding="20" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="📅 Sessions" FontSize="16" FontWeight="Bold" Foreground="#7B1FA2" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding UpcomingSessions}" FontSize="32" FontWeight="Bold" Foreground="#4A148C"/>
                        <TextBlock Text="Buổi tập sắp tới trong tháng" Foreground="#666" FontSize="12"/>
                    </StackPanel>
                </Border>
                
                <!-- Card 3: Members -->
                <Border Grid.Column="2" Background="#E8F5E9" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="👨‍👩‍👧 Members" FontSize="16" FontWeight="Bold" Foreground="#388E3C" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding TotalMembers}" FontSize="32" FontWeight="Bold" Foreground="#1B5E20"/>
                        <TextBlock Text="Thành viên tham gia" Foreground="#666" FontSize="12"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- Quick Actions -->
            <TextBlock Text="Hành động nhanh" FontSize="16" FontWeight="Bold" Foreground="#333" Margin="0,40,0,15"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Tạo team mới" Padding="12,10" Background="#4CAF50" 
                        Foreground="White" FontWeight="SemiBold" Margin="0,0,10,0" Cursor="Hand"
                        Command="{Binding DataContext.NavigateToTeamsCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                <Button Content="📋 Lịch tập của tôi" Padding="12,10" Background="#2196F3" 
                        Foreground="White" FontWeight="SemiBold" Cursor="Hand"
                        Command="{Binding DataContext.NavigateToPersonalScheduleCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="1"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
